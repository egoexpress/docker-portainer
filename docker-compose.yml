services:

  app:
    image: portainer/portainer-ee:2.35.0
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - data:/data
    networks:
      frontend:
      vpn:

networks:
  frontend:
  vpn:

volumes:
  data:
